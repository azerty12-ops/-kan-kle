## 2024-03-24 - Improve Core Command Discoverability
 **Learning:** In Telegram bots, relying purely on text-based `/commands` can create typing friction, especially on mobile devices.
 **Action:** Use `ReplyKeyboardMarkup` for core commands to present them as persistent, tap-friendly buttons, reducing friction and making features immediately discoverable to users.