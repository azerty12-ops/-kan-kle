## 2024-06-27 - Reduce typing friction with ReplyKeyboardMarkup
**Learning:** Telegram bot commands can be hard to discover and type on mobile devices, increasing user friction for core tasks.
**Action:** Use `ReplyKeyboardMarkup` with exact text match handlers (`filters.Regex`) to provide persistent, discoverable buttons for primary interactions.
