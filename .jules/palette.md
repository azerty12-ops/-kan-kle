## 2024-06-25 - Interactive Keyboards for Core Commands
**Learning:** Using `ReplyKeyboardMarkup` for core commands reduces typing friction and improves feature discoverability significantly in Telegram bots.
**Action:** Always provide custom keyboards for the most used features in the `/start` command instead of solely relying on the user typing or knowing `/commands`.
